spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: content_database

  datasource:
    jdbc-url: jdbc:postgresql://0.0.0.0:5432/user_database
    username: postgres
    password: postgres
